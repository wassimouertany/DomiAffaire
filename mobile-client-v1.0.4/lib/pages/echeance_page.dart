
import 'package:flutter/material.dart';

class EcheancePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Échéance Page'),
      ),
      body: Center(
        child: Text('Welcome to the Échéance Page'),
      ),
    );
  }
}